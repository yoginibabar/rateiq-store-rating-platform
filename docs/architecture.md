# Architecture

React + TypeScript + Vite frontend communicates with a REST API built with Node.js + Express + TypeScript. Prisma provides PostgreSQL persistence. JWT authentication and role-based middleware protect private routes. Analytics are implemented as deterministic, explainable service utilities.
